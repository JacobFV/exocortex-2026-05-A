{
  "name": "@exocortex/browser-session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @exocortex/protocol && tsx src/browser-session-manager.test.ts"
  },
  "dependencies": {
    "@exocortex/protocol": "0.0.0"
  }
}
